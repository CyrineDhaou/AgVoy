{% extends 'base.html.twig' %}

{% block title %}Etape{% endblock %}

{% block body %}
<body background="fond.jpg">
    <h1>Etape</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etape.id }}</td>
            </tr>
            <tr>
                <th>NumeroEtape</th>
                <td>{{ etape.numeroEtape }}</td>
            </tr>
            <tr>
                <th>VilleEtape</th>
                <td>{{ etape.villeEtape }}</td>
            </tr>
            <tr>
                <th>NombreJours</th>
                <td>{{ etape.nombreJours }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_etape_index') }}" class="btn btn-default">Back to list</a>
    <a href="{{ path('admin_etape_edit', {'id': etape.id}) }}" class="btn btn-default">Edit</a>

    {{ include('back/etape/_delete_form.html.twig') }}
    </body>
{% endblock %}
